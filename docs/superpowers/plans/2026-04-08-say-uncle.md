# Say Uncle Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a post-merge "Say Uncle" torture endurance challenge that replaces the immunity challenge — players endure dares, dominant performers pick victims (with backfire risk), last standing wins immunity, normal tribal follows.

**Architecture:** Single new engine function `simulateSayUncle(ep)` produces immunity winner + placements. Called from `simulateEpisode` when `ep.isSayUncle` is set — replaces `simulateIndividualChallenge` but lets the rest of the episode (tribal, voting, idols) run normally. Follows the Triple Dog Dare pattern for VP (sequential reveal). All code in `simulator.html`.

**Tech Stack:** Vanilla JS, single-file architecture (simulator.html)

**Reference patterns:**
- Engine: `simulateTripleDogDare(ep)` (~line 6488) — round-by-round challenge with reactions
- Twist catalog: `TWIST_CATALOG` (~line 1588) — entry format
- Challenge integration: Normal post-merge challenge at ~line 22054 — sets `ep.immunityWinner`, `ep.challengeType`, `ep.chalPlacements`
- VP: `rpBuildTripleDogDareRounds(ep)` — sequential click-to-reveal with data attributes

---

### Task 1: Twist Catalog Entry + Episode Flag

**Files:**
- Modify: `simulator.html` — `TWIST_CATALOG` (~line 1588), `applyTwist()` (~line 10353)

- [ ] **Step 1: Add TWIST_CATALOG entry**

Add after the `triple-dog-dare` entry (~line 1619):

```javascript
{ id:'say-uncle', emoji:'💪', name:'Say Uncle', category:'immunity', phase:'post-merge', desc:'Torture endurance challenge. Survive 10 seconds or you\'re out. Dominate a dare to pick the next victim — but if they pass, YOU\'RE eliminated instead. Last one standing wins immunity.', engineType:'say-uncle' },
```

- [ ] **Step 2: Add applyTwist handler**

In `applyTwist()`, find the `triple-dog-dare` handler. Add after it:

```javascript
} else if (engineType === 'say-uncle') {
  if (!gs.isMerged || gs.activePlayers.length < 3) return;
  ep.isSayUncle = true;
```

- [ ] **Step 3: Commit**

```bash
git add simulator.html
git commit -m "feat: add Say Uncle twist catalog entry and episode flag"
```

---

### Task 2: Dare Pool Constants

**Files:**
- Modify: `simulator.html` — add after `DARE_CATEGORIES` (~line 1702)

- [ ] **Step 1: Add SAY_UNCLE_POOL**

```javascript
// ── Say Uncle — endurance dare pools (survive 10 seconds) ──
const SAY_UNCLE_POOL = {
  'pain': [
    { title: 'Turtle Puck Shots', desc: 'Stand in a hockey net while Chef fires angry snapping turtles at you.' },
    { title: 'Electric Shock Chair', desc: 'Sit in the chair. Mild shocks every 2 seconds. Don\'t stand up.' },
    { title: 'Ant Hill Sit', desc: 'Sit on an active anthill for 10 seconds. No brushing them off.' },
    { title: 'Wooden Shorts', desc: 'Wear wooden shorts while a woodpecker goes to town.' },
    { title: 'Spike Bed', desc: 'Lay down on a bed of blunt spikes. Stay flat. 10 seconds.' },
    { title: 'Hot Coal Walk', desc: 'Walk barefoot across hot coals. One direction. No stopping.' },
    { title: 'Ice Bucket Burial', desc: 'Get buried under 50 pounds of ice. Stay still.' },
    { title: 'Water Balloon Headshots', desc: 'Stand still while Chef launches water balloons at your face.' },
    { title: 'Yellow Jacket', desc: 'Have your front covered in bees. Don\'t swat.' },
    { title: 'Grizzly Bear Log Roll', desc: 'Survive 10 seconds of log rolling against a grizzly bear.' },
    { title: 'Cactus Shirt', desc: 'Wear a shirt lined with cactus needles. Hug yourself.' },
    { title: 'Fire Ant Gloves', desc: 'Wear gloves full of fire ants. Keep them on.' },
    { title: 'Rubber Band Barrage', desc: 'Stand still while the tribe snaps rubber bands at you. 10 seconds.' },
    { title: 'Cattle Prod Poke', desc: 'One poke every 3 seconds. Four total. Don\'t move.' },
    { title: 'Frozen Hands', desc: 'Grip two blocks of ice. Hold on for 10 seconds.' },
    { title: 'Tar and Feather', desc: 'Get tarred and feathered. Stand there and take it.' },
    { title: 'Pepper Spray Breeze', desc: 'Stand downwind of a pepper spray fan. Eyes open.' },
    { title: 'Nail Bed Press', desc: 'Lay face-down on a bed of nails. Weight placed on your back.' },
    { title: 'Thunder Clap Drums', desc: 'Sit between two massive drums being hammered. Don\'t cover your ears.' },
    { title: 'Wrecking Ball Dodge', desc: 'Stand in a circle while a wrecking ball swings. Don\'t leave the circle.' },
  ],
  'fear': [
    { title: 'Snake Box', desc: 'Step into a box full of snakes. Stay inside. 10 seconds.' },
    { title: 'Sasquatchanakwa Crate', desc: 'Get inside a wooden crate with Sasquatchanakwa. Survive.' },
    { title: 'Jellyfish Pool', desc: 'Jump into a pool of jellyfish. Stay submerged.' },
    { title: 'Tarantula Face Walk', desc: 'Let a tarantula walk across your face. Mouth closed. Eyes open.' },
    { title: 'Scorpion Pit', desc: 'Stand in a pit of scorpions. Don\'t move.' },
    { title: 'Bat Cave Sit', desc: 'Sit in a dark cave full of bats. Don\'t run.' },
    { title: 'Wolf Staredown', desc: 'Make eye contact with a wolf for 10 seconds. Don\'t look away.' },
    { title: 'Shark Cage Dunk', desc: 'Get lowered into shark-infested water in a rusty cage.' },
    { title: 'Cliff Edge Blindfold', desc: 'Stand blindfolded on the edge of a cliff. Don\'t step back.' },
    { title: 'Coffin Close', desc: 'Lay in a coffin. Lid closes. 10 seconds in the dark.' },
    { title: 'Croc Teeth Cleaning', desc: 'Put your hand in a crocodile\'s mouth. Clean a tooth.' },
    { title: 'Dark Room', desc: 'Sit alone in a pitch-black room. Sounds play. Don\'t scream.' },
    { title: 'Rat Swarm', desc: 'Stand still while rats climb over you.' },
    { title: 'Piranha Pedicure', desc: 'Dip your feet in piranha water. They nibble. Don\'t pull out.' },
    { title: 'Wasp Nest Poke', desc: 'Poke a wasp nest with a short stick. Stand your ground.' },
    { title: 'Skunk Jump', desc: 'Jump over a line of skunks between rocks. Don\'t get sprayed.' },
    { title: 'Bull Pen', desc: 'Stand in a bull pen. Don\'t run. Don\'t wave anything red.' },
    { title: 'Bear Cave Nap', desc: 'Lay down at the entrance of a bear cave. Close your eyes.' },
    { title: 'Quicksand Stand', desc: 'Stand in quicksand up to your waist. Don\'t panic. Don\'t struggle.' },
    { title: 'Haunted Maze Sprint', desc: 'Sprint through a haunted maze. Finish in 10 seconds.' },
  ],
  'gross': [
    { title: 'Lake Leeches Barrel', desc: 'Sit in a barrel full of water and lake leeches.' },
    { title: 'Poison Ivy Spa', desc: 'Have your face wrapped in poison ivy. Don\'t scratch.' },
    { title: 'Nose Hair Pull', desc: 'Have all your nose hairs pulled at once. Don\'t flinch.' },
    { title: 'Manure Face Mask', desc: 'Get a face mask made of actual manure. Sit with it.' },
    { title: 'Got Milk?', desc: 'Attempt to milk an angry goat. Whatever happens, happens.' },
    { title: 'Goo Shoes', desc: 'Wear shoes filled with mystery goo. Walk 20 steps.' },
    { title: 'Worm Bath Soak', desc: 'Lay in a bathtub full of worms. Submerge.' },
    { title: 'Dumpster Dive Sit', desc: 'Sit inside a dumpster. Lid closes. Breathe through your mouth.' },
    { title: 'Maggot Massage', desc: 'Lay still while maggots are spread across your back.' },
    { title: 'Fish Scale Facial', desc: 'Get a facial made of fish scales and fish oil.' },
    { title: 'Sewage Snorkel', desc: 'Snorkel in murky swamp water. Face down. 10 seconds.' },
    { title: 'Slug Trail Necklace', desc: 'Wear a necklace of live slugs. They leave trails.' },
    { title: 'Mystery Goo Helmet', desc: 'Wear a helmet filled with unknown slime. It drips down your face.' },
    { title: 'Swamp Mud Burial', desc: 'Get buried in swamp mud up to your neck. Don\'t gag.' },
    { title: 'Bird Dropping Shower', desc: 'Stand under a birdcage. Look up. 10 seconds.' },
    { title: 'Catfish Kiss', desc: 'Kiss a live catfish. On the mouth. Hold it.' },
    { title: 'Rotten Egg Sauna', desc: 'Sit in a sauna filled with rotten egg smell. Breathe normally.' },
    { title: 'Cockroach Crown', desc: 'Wear a crown of live cockroaches on your head.' },
    { title: 'Blister Beetle Bracelet', desc: 'Wear a bracelet of blister beetles. They secrete. Don\'t remove it.' },
    { title: 'Roadkill Pillow', desc: 'Lay your head on a roadkill pillow. Close your eyes. 10 seconds.' },
  ],
  'humiliation': [
    { title: 'Cow Costume Parade', desc: 'Wear a cow costume. Parade around camp. Moo on command.' },
    { title: 'Wawanakwa Hair Salon', desc: 'Get a haircut from Chef. With a chainsaw.' },
    { title: 'Baby Diaper Dance', desc: 'Wear a diaper. Do a baby dance. In front of everyone.' },
    { title: 'Tickle Onslaught', desc: 'Endure one minute of constant tickling from two people.' },
    { title: 'Voice to Self', desc: 'Listen to a recording of your own voice on repeat. Don\'t cringe.' },
    { title: 'Marshmallow Waxing', desc: 'Get your face waxed using melted marshmallows.' },
    { title: 'Wedgie Marathon', desc: 'Give yourself a wedgie. Walk the length of camp. Waving.' },
    { title: 'Clown Makeover', desc: 'Full clown makeup. Nose. Wig. Do a routine.' },
    { title: 'Public Love Letter', desc: 'Read a love letter you wrote (to no one) aloud to the tribe.' },
    { title: 'Stand-Up About Yourself', desc: 'Do 60 seconds of stand-up comedy roasting yourself.' },
    { title: 'Dunce Cap Walk', desc: 'Wear a dunce cap. Sit in the corner. Everyone watches.' },
    { title: 'Sing Your Worst Moment', desc: 'Sing a song about your most embarrassing game moment.' },
    { title: 'Kiss the Fish', desc: 'Kiss a dead fish on camera. Maintain eye contact with the tribe.' },
    { title: 'Belly Flop Contest', desc: 'Do a belly flop off the dock. Maximum splash. Maximum pain.' },
    { title: 'Dance Battle Solo', desc: 'Dance battle against nobody. No music. Full commitment.' },
    { title: 'Serenade Your Enemy', desc: 'Serenade the person you like least. Be romantic.' },
    { title: 'Wear Rival\'s Clothes', desc: 'Wear your rival\'s clothes for the rest of the day. Their style.' },
    { title: 'Human Pinata', desc: 'Get hung up like a pinata. Tribe throws soft balls at you.' },
    { title: 'Truth Serum', desc: 'Answer any question the tribe asks. Honestly. For 60 seconds.' },
    { title: 'Walk of Shame Lap', desc: 'Walk a lap around camp while everyone slow-claps.' },
  ],
};
const SAY_UNCLE_CATEGORIES = Object.keys(SAY_UNCLE_POOL);
```

- [ ] **Step 2: Commit**

```bash
git add simulator.html
git commit -m "feat: add Say Uncle dare pool constants (80 dares, 4 categories)"
```

---

### Task 3: Engine Function — `simulateSayUncle(ep)`

**Files:**
- Modify: `simulator.html` — add before `simulateTripleDogDare` (~line 6486)

- [ ] **Step 1: Add the engine function**

```javascript
// ══════════════════════════════════════════════════════════════════════
// ENGINE: SAY UNCLE (NO PAIN NO GAME)
// ══════════════════════════════════════════════════════════════════════

function simulateSayUncle(ep) {
  const activePlayers = [...gs.activePlayers];
  const _rp = arr => arr[Math.floor(Math.random() * arr.length)];
  const maxRounds = 50; // safety cap

  // State tracking
  const remaining = [...activePlayers];
  const eliminated = []; // elimination order (first out = last place)
  const rounds = [];
  const backfires = [];
  let immunityWinner = null;

  // ── Helper: survival roll ──
  const _survivalRoll = (player, category, roundNum) => {
    const s = pStats(player);
    const fatigue = Math.pow(roundNum, 1.5) * 0.006;
    let primary = 0, secondary = 0;
    if (category === 'pain')        { primary = s.endurance * 0.07; secondary = s.physical * 0.04; }
    else if (category === 'fear')   { primary = s.boldness * 0.07;  secondary = s.endurance * 0.04; }
    else if (category === 'gross')  { primary = s.boldness * 0.07;  secondary = s.physical * 0.04; }
    else if (category === 'humiliation') { primary = s.boldness * 0.07; secondary = (10 - s.social) * 0.04; }
    const score = primary + secondary - fatigue + (Math.random() * 0.15);
    const passThreshold = 0.35;
    const dominantThreshold = 0.50;
    if (score >= dominantThreshold) return 'dominant';
    if (score >= passThreshold) return 'pass';
    return 'fail';
  };

  // ── Helper: reaction text ──
  const _surviveReaction = (player, result, dareTitle) => {
    const pr = pronouns(player);
    const arch = players.find(p => p.name === player)?.archetype || '';
    if (result === 'dominant') {
      const reactions = [
        `${player} didn't flinch. Didn't blink. Looked at the host and said "next."`,
        `${player} took it like it was nothing. The tribe is watching. ${pr.Sub} ${pr.sub === 'they' ? 'know' : 'knows'} they're watching.`,
        `${player} finished ${dareTitle} with a smirk. That wasn't a challenge. That was a warm-up.`,
      ];
      if (arch === 'villain' || arch === 'mastermind') reactions.push(`${player} smiled through the pain. That's the scary part.`);
      if (arch === 'hero') reactions.push(`${player} gritted ${pr.pos} teeth and powered through. Not a sound. The tribe respects that.`);
      return _rp(reactions);
    } else {
      const reactions = [
        `Teeth clenched. Eyes shut. Ten seconds never felt so long. But ${player} made it.`,
        `${player} was shaking by the end. But ${pr.sub} made it. Barely.`,
        `${player} survived ${dareTitle}. Not gracefully. But survival doesn't need to be graceful.`,
        `${player} hung on. The timer hit ten. ${pr.Sub} let out a breath ${pr.sub} didn't know ${pr.sub} ${pr.sub === 'they' ? 'were' : 'was'} holding.`,
      ];
      return _rp(reactions);
    }
  };

  const _failReaction = (player, dareTitle) => {
    const pr = pronouns(player);
    const reactions = [
      `${player} tapped out at 8 seconds. So close. Not close enough.`,
      `${player} said uncle before the timer hit 5. The body quit before the brain did.`,
      `${player} tried. You could see ${pr.obj} trying. But ${dareTitle} was too much.`,
      `${player} couldn't last. ${pr.Sub} ${pr.sub === 'they' ? 'step' : 'steps'} down. The stocks await.`,
      `${player} lasted 3 seconds. ${pr.Sub} ${pr.sub === 'they' ? 'look' : 'looks'} at the ground. "I can't."`,
    ];
    return _rp(reactions);
  };

  const _pickReaction = (picker, victim, confident) => {
    const pr = pronouns(picker);
    if (confident) {
      return _rp([
        `${picker} points at ${victim}. "Your turn." No hesitation.`,
        `${picker} picks ${victim} without blinking. ${pr.Sub} ${pr.sub === 'they' ? 'know' : 'knows'} ${victim}'s weakness.`,
        `"${victim}." ${picker} says the name like ${pr.sub} ${pr.sub === 'they' ? 'have' : 'has'} been waiting to say it.`,
      ]);
    } else {
      return _rp([
        `${picker} picks ${victim}. Bold move. Could backfire.`,
        `${picker} hesitates, then points at ${victim}. Not the safest choice.`,
        `"${victim}." ${picker} doesn't sound sure. The tribe notices.`,
      ]);
    }
  };

  const _backfireReaction = (picker, victim) => {
    return _rp([
      `The look on ${picker}'s face when ${victim} doesn't flinch. That backfire is going to sting longer than the dare.`,
      `${victim} walks out untouched. ${picker} walks to the stocks. The tribe saw everything.`,
      `${picker} picked wrong. ${victim} took it like nothing. Now ${picker}'s out. That's the game.`,
    ]);
  };

  const _calledItReaction = (picker, victim) => {
    return _rp([
      `${picker} called it. ${victim} couldn't handle it. The read was right.`,
      `${victim} breaks. ${picker} was counting on that. Smart play.`,
      `${picker} knew. Everybody watching knew. ${victim} wasn't built for that one.`,
    ]);
  };

  // ── Helper: victim targeting (dominator pick) ──
  const _pickVictim = (picker) => {
    const candidates = remaining.filter(p => p !== picker);
    if (!candidates.length) return null;
    const partners = new Set();
    (gs.namedAlliances || []).filter(a => a.active && a.members.includes(picker))
      .forEach(a => a.members.forEach(m => { if (m !== picker) partners.add(m); }));

    return wRandom(candidates, c => {
      const bond = getBond(picker, c);
      const heat = computeHeat(c, remaining, gs.namedAlliances || []);
      const s = pStats(c);
      // Pick weak players — they're more likely to fail (safe pick)
      const weakScore = ((10 - s.physical) * 0.1) + ((10 - s.endurance) * 0.1) + ((10 - s.boldness) * 0.1);
      // Challenge weakness from season history
      const chalRec = gs.chalRecord?.[c];
      const chalWeak = chalRec ? chalRec.bombs * 0.15 : 0;
      // Alliance protection
      const isPartner = partners.has(c);
      const partnerPenalty = isPartner ? -5.0 : 0;
      return Math.max(0.1, (-bond * 0.3) + (heat * 0.2) + weakScore + chalWeak + partnerPenalty + Math.random() * 0.2);
    });
  };

  // ── Helper: category pick (dominator chooses victim's weakest) ──
  const _pickCategory = (picker, victim) => {
    const s = pStats(victim);
    // Score each category — lower score = victim is weaker = better pick
    const scores = {
      pain: s.endurance * 0.07 + s.physical * 0.04,
      fear: s.boldness * 0.07 + s.endurance * 0.04,
      gross: s.boldness * 0.07 + s.physical * 0.04,
      humiliation: s.boldness * 0.07 + (10 - s.social) * 0.04,
    };
    // Pick the category where the victim scores lowest (most likely to fail)
    // But add some randomness — pickers don't have perfect information
    const pickerStrat = pStats(picker).strategic;
    const weighted = SAY_UNCLE_CATEGORIES.map(cat => ({
      cat, score: scores[cat] + (Math.random() * (1.1 - pickerStrat * 0.1) * 0.3)
    }));
    weighted.sort((a, b) => a.score - b.score);
    return weighted[0].cat; // lowest = victim's weakest
  };

  // ══════════════════════════════════════════════════════════════
  // MAIN ROUND LOOP
  // ══════════════════════════════════════════════════════════════
  let nextPlayer = null;  // null = random, playerName = dominator pick
  let nextCategory = null; // null = wheel spin, categoryName = dominator chose
  let pickedBy = null;     // who picked this player (for backfire tracking)

  for (let roundNum = 1; roundNum <= maxRounds; roundNum++) {
    if (remaining.length <= 1) break;

    // ── Final two: last dare decides the winner ──
    if (remaining.length === 2) {
      const finalist = nextPlayer || _rp(remaining);
      const other = remaining.find(p => p !== finalist);
      const category = nextCategory || _rp(SAY_UNCLE_CATEGORIES);
      const dareObj = _rp(SAY_UNCLE_POOL[category]);
      const result = _survivalRoll(finalist, category, roundNum);

      const round = {
        roundNum, player: finalist, pickedBy, pickerCategory: nextCategory,
        dareCategory: category, dareTitle: dareObj.title, dareText: dareObj.desc,
        result, isFinal: true,
        reaction: result !== 'fail' ? _surviveReaction(finalist, result, dareObj.title) : _failReaction(finalist, dareObj.title),
        backfire: null,
      };

      if (result !== 'fail') {
        // Finalist passes — they win
        immunityWinner = finalist;
        eliminated.push(other);
        if (pickedBy && pickedBy !== finalist) {
          // Backfire on the picker
          round.backfire = { picker: pickedBy, reaction: _backfireReaction(pickedBy, finalist) };
          // Picker already eliminated in previous round handling
        }
      } else {
        // Finalist fails — the other wins
        immunityWinner = other;
        eliminated.push(finalist);
        if (pickedBy) {
          round.calledIt = { picker: pickedBy, reaction: _calledItReaction(pickedBy, finalist) };
        }
      }
      rounds.push(round);
      break;
    }

    // ── Select player for this round ──
    const player = nextPlayer || _rp(remaining);
    const category = nextCategory || _rp(SAY_UNCLE_CATEGORIES);
    const dareObj = _rp(SAY_UNCLE_POOL[category]);

    // Reset for next round
    const _pickedBy = pickedBy;
    nextPlayer = null;
    nextCategory = null;
    pickedBy = null;

    // ── Attempt the dare ──
    const result = _survivalRoll(player, category, roundNum);

    const round = {
      roundNum, player, pickedBy: _pickedBy, pickerCategory: _pickedBy ? category : null,
      dareCategory: category, dareTitle: dareObj.title, dareText: dareObj.desc,
      result, isFinal: false,
      reaction: result !== 'fail' ? _surviveReaction(player, result, dareObj.title) : _failReaction(player, dareObj.title),
      backfire: null, calledIt: null, pick: null,
    };

    if (result === 'fail') {
      // ── FAILED — player is out ──
      remaining.splice(remaining.indexOf(player), 1);
      eliminated.push(player);

      if (_pickedBy) {
        // Picker called it right — victim failed
        round.calledIt = { picker: _pickedBy, reaction: _calledItReaction(_pickedBy, player) };
        addBond(player, _pickedBy, -0.2);
      }
      // Next round: random
    } else if (result === 'dominant') {
      // ── DOMINATED — player picks next victim + category ──
      if (_pickedBy) {
        // This was a picked dare — BACKFIRE on the picker
        round.backfire = { picker: _pickedBy, reaction: _backfireReaction(_pickedBy, player) };
        backfires.push({ picker: _pickedBy, victim: player, round: roundNum });
        remaining.splice(remaining.indexOf(_pickedBy), 1);
        eliminated.push(_pickedBy);
        addBond(_pickedBy, player, -0.3);
        addBond(player, _pickedBy, 0.3);
      }
      // Player picks next victim
      const victim = _pickVictim(player);
      if (victim) {
        const victimCategory = _pickCategory(player, victim);
        const victimStats = pStats(victim);
        const isConfident = victimStats.physical <= 5 || victimStats.endurance <= 5 || victimStats.boldness <= 5;
        round.pick = {
          victim, category: victimCategory,
          reaction: _pickReaction(player, victim, isConfident),
          categoryReason: `${player} targets ${victim}'s weakness: ${victimCategory}.`,
        };
        nextPlayer = victim;
        nextCategory = victimCategory;
        pickedBy = player;
      }
    } else {
      // ── PASSED (normal) — survived but didn't dominate ──
      if (_pickedBy) {
        // This was a picked dare — BACKFIRE on the picker (victim passed)
        round.backfire = { picker: _pickedBy, reaction: _backfireReaction(_pickedBy, player) };
        backfires.push({ picker: _pickedBy, victim: player, round: roundNum });
        remaining.splice(remaining.indexOf(_pickedBy), 1);
        eliminated.push(_pickedBy);
        addBond(_pickedBy, player, -0.3);
        addBond(player, _pickedBy, 0.3);
        // Victim who caused backfire can now pick (they earned it)
        const bfVictim = _pickVictim(player);
        if (bfVictim) {
          const bfCategory = _pickCategory(player, bfVictim);
          const bfStats = pStats(bfVictim);
          const bfConfident = bfStats.physical <= 5 || bfStats.endurance <= 5 || bfStats.boldness <= 5;
          round.pick = {
            victim: bfVictim, category: bfCategory,
            reaction: _pickReaction(player, bfVictim, bfConfident),
            categoryReason: `${player} earned the pick from the backfire. Targets ${bfVictim}'s weakness: ${bfCategory}.`,
          };
          nextPlayer = bfVictim;
          nextCategory = bfCategory;
          pickedBy = player;
        }
      }
      // If not a picked dare, just survived — next round random
    }

    rounds.push(round);
  }

  // If somehow we hit maxRounds, last remaining player wins
  if (!immunityWinner && remaining.length) {
    immunityWinner = remaining[0];
  }

  // Build placements: winner first, then reverse elimination order
  const placements = [immunityWinner, ...eliminated.reverse()].filter(Boolean);

  // Set results on ep
  ep.sayUncle = {
    rounds, placements, backfires,
    eliminated: [...eliminated].reverse(), // re-reverse to elimination order
    immunityWinner,
    playerCount: activePlayers.length,
  };

  // Set standard challenge fields so the rest of the episode flow works
  ep.immunityWinner = immunityWinner;
  ep.challengeType = 'individual';
  ep.challengeLabel = 'Say Uncle';
  ep.challengeCategory = 'endurance';
  ep.challengeDesc = 'Torture endurance challenge — last one standing wins immunity.';
  ep.chalPlacements = placements;
  ep.chalMemberScores = {};
  placements.forEach((name, i) => { ep.chalMemberScores[name] = placements.length - i; });

  // Camp events
  const campKey = gs.mergeName || 'merge';
  if (!ep.campEvents) ep.campEvents = {};
  if (!ep.campEvents[campKey]) ep.campEvents[campKey] = { pre: [], post: [] };

  // Winner
  if (immunityWinner) {
    ep.campEvents[campKey].post.push({
      type: 'sayUncleWinner', players: [immunityWinner],
      text: `${immunityWinner} outlasted everyone in Say Uncle. That's immunity.`,
      badgeText: 'LAST ONE STANDING', badgeClass: 'gold'
    });
  }

  // Backfires
  backfires.forEach(bf => {
    ep.campEvents[campKey].post.push({
      type: 'sayUncleBackfire', players: [bf.picker, bf.victim],
      text: `${bf.picker} picked ${bf.victim} — and it backfired. ${bf.victim} passed. ${bf.picker} is out.`,
      badgeText: 'BACKFIRE', badgeClass: 'red'
    });
  });

  // Dominant performances
  rounds.filter(r => r.result === 'dominant' && !r.isFinal).forEach(r => {
    ep.campEvents[campKey].post.push({
      type: 'sayUncleDominated', players: [r.player],
      text: `${r.player} dominated ${r.dareTitle}. Didn't flinch.`,
      badgeText: 'DIDN\'T FLINCH', badgeClass: 'gold'
    });
  });
}
```

- [ ] **Step 2: Commit**

```bash
git add simulator.html
git commit -m "feat: add simulateSayUncle engine function"
```

---

### Task 4: Episode Flow Integration

**Files:**
- Modify: `simulator.html` — challenge execution in `simulateEpisode` (~line 22054), `patchEpisodeHistory` (~line 29591), camp event boosts

- [ ] **Step 1: Intercept the normal challenge with Say Uncle**

Find the normal post-merge individual challenge at ~line 22054:
```javascript
    } else {
    const _chalPool = gs.activePlayers.filter(p => p !== gs.exileDuelPlayer);
    const immResult = simulateIndividualChallenge(_chalPool, null);
    ep.challengeType = 'individual'; ep.immunityWinner = immResult?.name || null;
```

Add BEFORE the `} else {` line:

```javascript
    } else if (ep.isSayUncle) {
    // Say Uncle replaces the immunity challenge — normal tribal follows
    simulateSayUncle(ep);
    // challengeType, immunityWinner, chalPlacements already set by simulateSayUncle
```

- [ ] **Step 2: Add to patchEpisodeHistory**

Near the `tripleDogDare` patch (~line 29591), add:

```javascript
  if (ep.sayUncle) h.sayUncle = ep.sayUncle;
  if (ep.isSayUncle) h.isSayUncle = true;
```

- [ ] **Step 3: Add camp event boost**

Near the `triple-dog-dare` case in the boost switch, add:

```javascript
      case 'say-uncle':
        boost('doubt', 25); boost('confessional', 30);
        boost('strategicTalk', 20); boost('fight', 15);
        break;
```

Note: Say Uncle uses `ep.challengeType = 'individual'` (not a custom type) so the existing challenge VP screens and tribal flow work normally. The `ep.isSayUncle` flag is only for the Say Uncle-specific VP screens.

- [ ] **Step 4: Exclude from generic twist screen**

Find the twist screen filter (~line 35629) that excludes `triple-dog-dare`. Add `say-uncle`:

Add `&& t.type !== 'say-uncle'` to the filter chain.

- [ ] **Step 5: Commit**

```bash
git add simulator.html
git commit -m "feat: integrate Say Uncle into episode flow"
```

---

### Task 5: Camp Event Badges

**Files:**
- Modify: `simulator.html` — badge chain in `rpBuildCampTribe()`

- [ ] **Step 1: Add event type detection**

After the TDD dare badge detections, add:

```javascript
    const isSayUncleWin   = evt.type === 'sayUncleWinner';
    const isSayUncleDom   = evt.type === 'sayUncleDominated';
    const isSayUncleBack  = evt.type === 'sayUncleBackfire';
    const isSayUncleCalled= evt.type === 'sayUncleCalledIt';
    const isSayUncleFail  = evt.type === 'sayUncleFailed';
    const isSayUncleEndure= evt.type === 'sayUncleEndured';
```

- [ ] **Step 2: Add badge text**

After TDD badge text entries:

```javascript
                     : isSayUncleWin    ? (evt.badgeText || 'LAST ONE STANDING')
                     : isSayUncleDom    ? (evt.badgeText || 'DIDN\'T FLINCH')
                     : isSayUncleBack   ? (evt.badgeText || 'BACKFIRE')
                     : isSayUncleCalled ? (evt.badgeText || 'CALLED IT')
                     : isSayUncleFail   ? (evt.badgeText || 'SAID UNCLE')
                     : isSayUncleEndure ? (evt.badgeText || 'ENDURED')
```

- [ ] **Step 3: Add badge class**

After TDD badge class entries:

```javascript
                     : isSayUncleWin || isSayUncleDom || isSayUncleEndure ? 'gold'
                     : isSayUncleBack || isSayUncleFail || isSayUncleCalled ? 'red'
```

- [ ] **Step 4: Commit**

```bash
git add simulator.html
git commit -m "feat: add Say Uncle camp event badges"
```

---

### Task 6: VP Screens — Announcement + Rounds + Immunity

**Files:**
- Modify: `simulator.html` — add VP functions before the TDD VP section

- [ ] **Step 1: Add announcement screen**

Add before `// TRIPLE DOG DARE VP SCREENS`:

```javascript
// ══════════════════════════════════════════════════════════════════════
// SAY UNCLE VP SCREENS
// ══════════════════════════════════════════════════════════════════════

function rpBuildSayUncleAnnouncement(ep) {
  const su = ep.sayUncle;
  if (!su) return '';
  const prevEp = (gs.episodeHistory || []).filter(h => h.num < ep.num).slice(-1)[0];
  const prevSnap = prevEp?.gsSnapshot || {};
  const snap = ep.gsSnapshot || {};
  const _tm = ep.tribesAtStart?.flatMap(t => t.members);
  const activePlayers = (_tm?.length ? _tm : null) || prevSnap.activePlayers || snap.activePlayers || [];

  const _hostQuotes = [
    'Today\'s challenge is called Say Uncle. You\'ll each face a random torture. Survive ten seconds, and you move on. Quit, and you\'re done.',
    'The Wheel of Misfortune decides your fate. Last one standing wins immunity. But if you dominate your dare — you get to pick who goes next. Choose wisely. If they pass, you\'re out instead.',
    'No alliances can save you here. No strategy. Just pain tolerance and the will to keep going.',
  ];
  const _hostQuote = _hostQuotes[Math.floor(Math.abs((ep.num || 1) * 23) % _hostQuotes.length)];

  let html = `<div class="rp-page tod-arena">
    <div style="font-family:var(--font-display);font-size:32px;letter-spacing:3px;text-align:center;color:var(--accent-fire);text-shadow:0 0 20px var(--accent-fire);margin-bottom:4px;animation:scrollDrop 0.5s var(--ease-broadcast) both">SAY UNCLE</div>
    <div style="font-size:10px;font-weight:700;letter-spacing:3px;text-transform:uppercase;color:var(--muted);text-align:center;margin-bottom:24px">Torture Endurance Challenge</div>

    <div style="font-size:13px;color:var(--vp-text);text-align:center;margin-bottom:24px;line-height:1.7;max-width:460px;margin-left:auto;margin-right:auto;font-style:italic">
      "${_hostQuote}"
    </div>

    <div style="display:flex;flex-wrap:wrap;gap:16px;justify-content:center;margin-bottom:24px;max-width:500px;margin-left:auto;margin-right:auto">
      <div class="vp-card" style="flex:1;min-width:180px;text-align:center;padding:12px">
        <div style="font-size:9px;font-weight:700;letter-spacing:2px;color:var(--accent-gold);margin-bottom:8px">THE RULES</div>
        <div style="font-size:11px;color:var(--muted);line-height:1.6;text-align:left">
          <div style="margin-bottom:4px"><span style="color:#3fb950;font-weight:700">Survive</span> 10 seconds — move on</div>
          <div style="margin-bottom:4px"><span style="color:var(--accent-gold);font-weight:700">Dominate</span> — pick the next victim + dare</div>
          <div style="margin-bottom:4px"><span style="color:#da3633;font-weight:700">Victim passes</span> — YOU'RE out (backfire)</div>
          <div><span style="color:#da3633;font-weight:700">Fail</span> — eliminated from challenge</div>
        </div>
      </div>
      <div class="vp-card" style="flex:1;min-width:180px;text-align:center;padding:12px">
        <div style="font-size:9px;font-weight:700;letter-spacing:2px;color:var(--accent-fire);margin-bottom:8px">THE STAKES</div>
        <div style="font-size:11px;color:var(--muted);line-height:1.6">
          <div style="margin-bottom:4px">${su.playerCount} players</div>
          <div style="margin-bottom:4px">${su.rounds.length} rounds</div>
          <div style="color:var(--accent-gold);font-weight:700">Winner gets immunity</div>
          <div style="color:var(--muted)">Normal tribal follows</div>
        </div>
      </div>
    </div>

    <div style="font-size:9px;font-weight:700;letter-spacing:2px;color:var(--muted);text-align:center;margin-bottom:10px">CONTESTANTS</div>
    <div class="rp-portrait-row" style="justify-content:center">
      ${activePlayers.map(name => rpPortrait(name)).join('')}
    </div>
  </div>`;
  return html;
}
```

- [ ] **Step 2: Add rounds screen (sequential click-to-reveal)**

```javascript
function rpBuildSayUncleRounds(ep) {
  const su = ep.sayUncle;
  if (!su || !su.rounds?.length) return '';
  const uid = 'su-' + ep.num;
  const _catColor = { 'pain': '#da3633', 'fear': '#8957e5', 'gross': '#3fb950', 'humiliation': '#db61a2' };

  // Build reveal items
  const revealItems = [];

  su.rounds.forEach(round => {
    const catColor = _catColor[round.dareCategory] || '#8b949e';
    const isBackfire = !!round.backfire;
    const isFinal = round.isFinal;

    let roundHtml = '';

    // Round header
    roundHtml += `<div style="font-size:10px;font-weight:700;color:var(--muted);font-family:var(--font-mono);margin-bottom:8px">${isFinal ? 'FINAL ROUND' : 'ROUND ' + round.roundNum}</div>`;

    // Who's up + how they were selected
    if (round.pickedBy) {
      roundHtml += `<div style="font-size:11px;font-weight:700;color:#da3633;letter-spacing:0.5px;margin-bottom:6px">PICKED BY ${round.pickedBy.toUpperCase()}</div>`;
    }

    // Player portrait + dare
    roundHtml += `<div style="display:flex;align-items:center;gap:8px;margin-bottom:8px">
      ${rpPortrait(round.player, 'sm')}
      <div style="font-size:12px;color:var(--vp-text)">${round.player} faces the Wheel of Misfortune\u2026</div>
    </div>
    <div style="background:${catColor}08;border-left:3px solid ${catColor};padding:8px 12px;margin-bottom:12px;border-radius:0 6px 6px 0">
      <div style="font-size:14px;font-weight:700;color:var(--vp-text);margin-bottom:4px">${round.dareTitle}</div>
      <div style="font-size:12px;color:var(--muted);line-height:1.5">${round.dareText}</div>
    </div>`;

    // Result
    if (round.result === 'dominant') {
      roundHtml += `<div style="font-size:11px;font-weight:700;color:var(--accent-gold);letter-spacing:0.5px;margin-bottom:4px">\u2B50 DOMINATED \u2014 DIDN'T FLINCH</div>`;
    } else if (round.result === 'pass') {
      roundHtml += `<div style="font-size:11px;font-weight:700;color:#3fb950;letter-spacing:0.5px;margin-bottom:4px">\u2705 SURVIVED</div>`;
    } else {
      roundHtml += `<div style="font-size:11px;font-weight:700;color:#da3633;letter-spacing:0.5px;margin-bottom:4px">\u274c SAID UNCLE</div>`;
    }

    // Reaction text
    if (round.reaction) {
      roundHtml += `<div style="font-size:12px;color:var(--vp-text);line-height:1.5;margin-bottom:8px">${round.reaction}</div>`;
    }

    // Backfire moment
    if (round.backfire) {
      roundHtml += `<div style="border-top:1px solid var(--border);padding-top:8px;margin-top:8px">
        <div style="font-size:11px;font-weight:700;color:#da3633;letter-spacing:1px;margin-bottom:4px">\uD83D\uDCA5 BACKFIRE</div>
        <div style="display:flex;align-items:center;gap:8px;margin-bottom:4px">
          ${rpPortrait(round.backfire.picker, 'sm', 'elim')}
          <div style="font-size:12px;color:var(--vp-text);line-height:1.5">${round.backfire.reaction}</div>
        </div>
      </div>`;
    }

    // Called it
    if (round.calledIt) {
      roundHtml += `<div style="border-top:1px solid var(--border);padding-top:8px;margin-top:8px">
        <div style="font-size:11px;font-weight:700;color:var(--accent-gold);letter-spacing:1px;margin-bottom:4px">\u2705 CALLED IT</div>
        <div style="font-size:12px;color:var(--muted);line-height:1.5">${round.calledIt.reaction}</div>
      </div>`;
    }

    // Pick announcement (dominator picks next victim)
    if (round.pick) {
      roundHtml += `<div style="border-top:1px solid var(--border);padding-top:8px;margin-top:8px">
        <div style="font-size:11px;font-weight:700;color:var(--accent-fire);letter-spacing:1px;margin-bottom:4px">\uD83C\uDFAF PICKS NEXT VICTIM</div>
        <div style="display:flex;align-items:center;gap:8px;margin-bottom:4px">
          ${rpPortrait(round.player, 'sm')}
          <span style="font-size:14px;color:var(--accent-fire)">\u2192</span>
          ${rpPortrait(round.pick.victim, 'sm')}
        </div>
        <div style="font-size:12px;color:var(--vp-text);line-height:1.5;margin-bottom:4px">${round.pick.reaction}</div>
        <div style="font-size:10px;color:var(--muted);font-style:italic">${round.pick.categoryReason}</div>
      </div>`;
    }

    const cardClass = round.result === 'fail' ? 'fire' : round.backfire ? 'fire' : round.result === 'dominant' ? 'gold' : '';
    revealItems.push({ type: 'round',
      html: `<div class="vp-card ${cardClass}" style="margin-bottom:12px">${roundHtml}</div>` });
  });

  // Serialize and build page
  let html = `<div class="rp-page tod-arena" id="${uid}-page"
    data-su-revealed="0" data-su-total="${revealItems.length}">
    <div style="font-family:var(--font-display);font-size:22px;letter-spacing:2px;text-align:center;color:var(--accent-fire);margin-bottom:16px">THE TORTURE</div>`;

  // Remaining / stocks tracker
  html += `<div style="font-size:10px;color:var(--muted);text-align:center;margin-bottom:16px">${su.playerCount} players \u2014 ${su.rounds.length} rounds</div>`;

  // Hidden reveal items
  revealItems.forEach((item, i) => {
    html += `<div class="${uid}-item" style="display:none" data-idx="${i}">${item.html}</div>`;
  });

  // Reveal buttons
  html += `<div style="display:flex;gap:10px;justify-content:center;margin-top:16px;position:sticky;bottom:10px;z-index:5">
    <button class="rp-camp-toggle-btn" style="border-color:var(--accent-fire);color:var(--accent-fire);padding:8px 20px;font-size:12px" onclick="suRevealNext('${uid}')">NEXT \u25B6</button>
    <button class="rp-camp-toggle-btn" style="border-color:#484f58;color:#8b949e;padding:8px 16px;font-size:11px" onclick="suRevealAll('${uid}')">REVEAL ALL</button>
  </div>`;

  html += `</div>`;
  return html;
}
```

- [ ] **Step 3: Add immunity/placements screen**

```javascript
function rpBuildSayUncleImmunity(ep) {
  const su = ep.sayUncle;
  if (!su || !su.immunityWinner) return '';
  const winner = su.immunityWinner;
  const p = players.find(x => x.name === winner);
  const arch = p?.archetype || 'player';
  const archLabel = arch.replace(/-/g, ' ').replace(/\b\w/g, c => c.toUpperCase());

  let html = `<div class="rp-page tod-arena" style="text-align:center">
    <div class="rp-co-eyebrow" style="color:var(--accent-gold);margin-bottom:20px">IMMUNITY WINNER</div>
    ${rpPortrait(winner, 'lg')}
    <div class="rp-elim-name" style="margin-top:16px">${winner}</div>
    <div class="rp-elim-arch">${archLabel}</div>
    <div style="font-size:12px;color:var(--accent-gold);font-weight:700;letter-spacing:2px;margin-top:8px">LAST ONE STANDING</div>

    <div class="rp-co-divider"></div>
    <div style="font-size:9px;font-weight:700;letter-spacing:2px;color:var(--muted);margin-bottom:12px">PLACEMENT ORDER</div>`;

  su.placements.forEach((name, i) => {
    const isWinner = i === 0;
    const isBackfire = su.backfires.some(bf => bf.picker === name);
    const badge = isWinner ? 'WINNER' : isBackfire ? 'BACKFIRE' : `#${i + 1}`;
    const badgeColor = isWinner ? 'var(--accent-gold)' : isBackfire ? '#da3633' : 'var(--muted)';
    html += `<div style="display:flex;align-items:center;gap:8px;padding:4px 0;${i > 0 ? 'opacity:0.7' : ''}">
      <span style="font-size:10px;font-weight:700;color:${badgeColor};font-family:var(--font-mono);width:70px;text-align:right;flex-shrink:0">${badge}</span>
      ${rpPortrait(name, 'sm', i > 0 ? 'elim' : '')}
      <span style="font-size:12px;color:var(--vp-text)">${name}</span>
    </div>`;
  });

  html += `</div>`;
  return html;
}
```

- [ ] **Step 4: Add global reveal functions**

Near the `tddRevealNext` / `tddRevealAll` functions, add:

```javascript
// ── Say Uncle sequential reveal ──
function suRevealNext(uid) {
  const page = document.getElementById(uid + '-page');
  if (!page) return;
  const revealed = parseInt(page.dataset.suRevealed) || 0;
  const total = parseInt(page.dataset.suTotal) || 0;
  if (revealed >= total) return;
  const el = document.querySelector('.' + uid + '-item[data-idx="' + revealed + '"]');
  if (el) { el.style.display = 'block'; el.scrollIntoView({ behavior: 'smooth', block: 'nearest' }); }
  page.dataset.suRevealed = revealed + 1;
}
function suRevealAll(uid) {
  const page = document.getElementById(uid + '-page');
  if (!page) return;
  const total = parseInt(page.dataset.suTotal) || 0;
  while ((parseInt(page.dataset.suRevealed) || 0) < total) suRevealNext(uid);
}
```

- [ ] **Step 5: Commit**

```bash
git add simulator.html
git commit -m "feat: add Say Uncle VP screens (announcement, rounds, immunity)"
```

---

### Task 7: VP Screen Registration

**Files:**
- Modify: `simulator.html` — `buildVPScreens()` (~line 46853)

- [ ] **Step 1: Register Say Uncle VP screens**

Find the Triple Dog Dare VP registration. Add BEFORE it:

```javascript
  // ── Say Uncle — replaces challenge, shows before normal challenge screen ──
  if (ep.isSayUncle && ep.sayUncle) {
    vpScreens.push({ id:'su-announce', label:'Say Uncle', html: rpBuildSayUncleAnnouncement(ep) });
    vpScreens.push({ id:'su-rounds', label:'The Torture', html: rpBuildSayUncleRounds(ep) });
    vpScreens.push({ id:'su-immunity', label:'Immunity', html: rpBuildSayUncleImmunity(ep) });
  }
```

Also: the normal challenge screen should NOT render when Say Uncle ran (it replaces the challenge). Find the guard that skips challenge screens for slasher/TDD (~line 46628):
```javascript
} else if (ep.challengeType && !ep.isFinale && !ep.isSlasherNight && !ep.isTripleDogDare) {
```
Add `&& !ep.isSayUncle`:
```javascript
} else if (ep.challengeType && !ep.isFinale && !ep.isSlasherNight && !ep.isTripleDogDare && !ep.isSayUncle) {
```

- [ ] **Step 2: Commit**

```bash
git add simulator.html
git commit -m "feat: register Say Uncle VP screens"
```

---

### Task 8: Text Backlog + Cold Open + Episode History

**Files:**
- Modify: `simulator.html` — text backlog, cold open, episode history badge

- [ ] **Step 1: Add text backlog function**

Before `_textTripleDogDare`:

```javascript
// ── SAY UNCLE ──
function _textSayUncle(ep, ln, sec) {
  const su = ep.sayUncle;
  if (!su) return;
  sec('SAY UNCLE');
  ln(`${su.playerCount} players. ${su.rounds.length} rounds. Torture endurance.`);
  ln('');

  su.rounds.forEach(round => {
    const picked = round.pickedBy ? ` [PICKED BY ${round.pickedBy}]` : '';
    ln(`Round ${round.roundNum}: ${round.player}${picked} — [${round.dareCategory.toUpperCase()}] ${round.dareTitle}`);
    ln(`  Result: ${round.result.toUpperCase()}`);
    if (round.backfire) ln(`  BACKFIRE: ${round.backfire.picker} is OUT`);
    if (round.calledIt) ln(`  CALLED IT: ${round.calledIt.picker} was right`);
    if (round.pick) ln(`  PICKS: ${round.pick.victim} (${round.pick.category})`);
  });
  ln('');

  if (su.backfires.length) {
    ln('BACKFIRES:');
    su.backfires.forEach(bf => ln(`- Round ${bf.round}: ${bf.picker} picked ${bf.victim} — backfired`));
    ln('');
  }

  ln('PLACEMENTS:');
  su.placements.forEach((name, i) => ln(`  ${i + 1}. ${name}${i === 0 ? ' (IMMUNITY)' : ''}`));
}
```

- [ ] **Step 2: Wire into main text output**

Find where `_textTripleDogDare(ep, ln, sec)` is called. Add before it:

```javascript
  _textSayUncle(ep, ln, sec);
```

- [ ] **Step 3: Add cold open card**

In the "Previously on..." section, after the TDD cold open card, add:

```javascript
    // ── [1c] SAY UNCLE RECAP ──
    if (prevEp.isSayUncle && prevEp.sayUncle) {
      const _suData = prevEp.sayUncle;
      html += `<div class="vp-card gold" style="margin-bottom:10px">
        <div style="font-family:var(--font-body);font-size:11px;font-weight:800;letter-spacing:1px;color:var(--accent-gold);margin-bottom:6px">SAY UNCLE</div>
        <div style="font-size:12px;margin-bottom:4px">${_suData.immunityWinner} won immunity — last one standing after ${_suData.rounds.length} rounds</div>
        ${_suData.backfires?.length ? `<div style="font-size:11px;color:#da3633">${_suData.backfires.length} backfire${_suData.backfires.length > 1 ? 's' : ''} — picks that blew up</div>` : ''}
      </div>`;
    }
```

- [ ] **Step 4: Add episode history badge**

Find where the TDD episode history badge is rendered (~line 30243). Add a Say Uncle badge:

```javascript
const suTag = ep.isSayUncle ? `<span class="ep-hist-tag" style="background:rgba(245,158,11,0.15);color:#f59e0b">Say Uncle</span>` : '';
```

And append `${suTag}` where `${tddTag}` is appended.

- [ ] **Step 5: Commit**

```bash
git add simulator.html
git commit -m "feat: add Say Uncle text backlog, cold open, episode history badge"
```

---

### Task 9: Testing & Polish

- [ ] **Step 1: Test basic flow**

Schedule `say-uncle` on a post-merge episode. Run to that episode. Verify:
- Say Uncle runs instead of normal immunity challenge
- VP shows: announcement → rounds (click-to-reveal) → immunity winner
- Normal tribal follows after (voting plans, votes, elimination)
- Immunity winner is protected at tribal

- [ ] **Step 2: Test backfire**

Run multiple seasons until a backfire occurs. Verify:
- Dominant player picks a victim
- Victim passes → picker is eliminated from challenge
- VP shows the BACKFIRE moment dramatically
- Placements show backfire victims marked

- [ ] **Step 3: Test edge cases**

- Schedule on pre-merge → should skip
- Schedule with < 3 players → should skip
- Final two round → one gets a dare, winner decided

- [ ] **Step 4: Commit any fixes**

```bash
git add simulator.html
git commit -m "fix: polish Say Uncle edge cases"
```
